zh_init.h

      // \
      // ZH_EXTERN_BEGIN \
      // void ext_##func( void ) \
      // { \
      //    symbols = zh_vmProcessSymbols( symbols_table, ( ZH_USHORT ) ZH_INIT_SYMBOLS_COUNT, (module), (id), (vpcode) ); \
      //    return 0; \
      // } \
      // ZH_EXTERN_END